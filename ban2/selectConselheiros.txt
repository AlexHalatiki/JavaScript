select * from Estudante inner join Conselheiro using(matricula) where Estudante.curso = ?

public List<Estudante> conselheiros(int curso) throws SelectException {
	List<Estudante> conselheiros = new List<Estudante>();
	try{
		selectConselheiros.setInt(1,curso);
		ResultSet rs = selectConselheiros.executeQuery();
		while(rs.next()){
			Estudante conselheiro = new Estudante();
			...
			conselheiros.add(conselheiro);
		}
		return conselheiros;
	}
	catch(Exception ex){
		throw new SelectException("Erro ao encontrar conselheiros disponíveis");
	}
}